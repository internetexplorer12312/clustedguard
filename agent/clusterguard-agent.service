[Unit]
Description=ClusterGuard metrics agent
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/clusterguard-agent -addr :9100
Environment=CLUSTERGUARD_TOKEN=change-me
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
